package exercises;

public class ex41 {
    public static void main(String[] args) {
        double a = 0;
        double x;
        if (a >= 0) x = 1.701;
        if (a < 0) x = 2D*3.14f;
        // System.out.println(x);

        // the double x variable was never initialized,
        // so it cannot be changed.
    }
}
